# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @nx-repo/backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nx-repo-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system nx-repo-backend && \
          adduser --system -G nx-repo-backend nx-repo-backend

COPY apps/backend/dist nx-repo-backend/
COPY apps/backend/package.json nx-repo-backend/
RUN chown -R nx-repo-backend:nx-repo-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix nx-repo-backend --omit=dev -f install

CMD [ "node", "nx-repo-backend" ]
